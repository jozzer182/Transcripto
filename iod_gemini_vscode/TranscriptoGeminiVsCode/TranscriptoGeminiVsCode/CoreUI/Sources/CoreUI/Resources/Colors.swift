import SwiftUI

public extension Color {
    static let background = Color("BackgroundColor", bundle: .module)
    static let cardBackground = Color("CardBackgroundColor", bundle: .module)
    static let primaryText = Color("PrimaryTextColor", bundle: .module)
    static let secondaryText = Color("SecondaryTextColor", bundle: .module)
    static let accent = Color("AccentColor", bundle: .module)
    static let error = Color("ErrorColor", bundle: .module)
}
